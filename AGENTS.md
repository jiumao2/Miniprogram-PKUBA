# AGENTS.md

本文件用于说明本仓库的基本结构、常见维护方式，以及前端页面修改时需要遵守的风格约定。

## 项目概览

这是一个微信小程序项目，主要用于北大篮协相关赛事的赛程管理与信息展示，包含：

- 首页导航
- 赛程查询
- 积分表查询
- 淘汰赛对阵
- 领队端操作
- 管理员端操作
- 云函数数据读取与更新

## 仓库结构

### 1. 小程序前端

前端代码位于 `miniprogram/` 下：

- `miniprogram/app.js`
  小程序全局逻辑与全局数据初始化。
- `miniprogram/app.json`
  页面注册、窗口配置、扩展库配置。
- `miniprogram/app.wxss`
  全局样式。
- `miniprogram/pages/`
  各页面目录，每个页面通常包含：
  - `xxx.wxml`
  - `xxx.js`
  - `xxx.wxss`
  - `xxx.json`

### 2. 云函数

云函数代码位于 `cloudfunctions/` 下。每个子目录对应一个云函数，用于：

- 查询赛程
- 查询淘汰赛
- 获取元数据
- 处理申请或管理端操作

前端页面若依赖数据库内容，通常会通过 `wx.cloud.callFunction` 调用这里的逻辑。

### 3. 数据与脚本

仓库根目录有若干数据文件和 Matlab 脚本，例如：

- `schedule.json`
- `team.json`
- `xls2Json_fall.m`
- `xls2Json_spring.m`

这些文件主要用于赛程、队伍、表格导入导出或数据转换。

## 常见更新内容

这个项目日常维护中，最常见的改动通常不是大规模重构，而是页面内容和入口调整，尤其是按钮相关更新。

重点注意：

- 首页和各功能页的按钮文案
- 按钮跳转目标是否正确
- 新增功能时是否需要增加入口按钮
- 管理员页、领队页是否同步补充按钮
- 按钮顺序是否符合当前使用习惯

如果用户说“更新一下页面”或“改一下首页”，优先检查：

1. `miniprogram/pages/index/index.wxml`
2. 对应页面的 `wxml`
3. 是否涉及 `bindtap`
4. 对应 `js` 中的方法是否存在
5. 跳转路径是否正确

## 页面修改原则

### 1. 先保证功能，再调整样式

任何样式优化都不能影响原有跳转、数据渲染和按钮点击逻辑。

### 2. 尽量小改

这个项目更适合稳妥维护，不适合无必要地大改页面结构。除非用户明确要求重构，否则优先：

- 保持原有 DOM 结构
- 保持原有字段命名
- 保持原有交互方式

### 3. 修改按钮时优先关注一致性

按钮修改需要保持：

- 文案统一
- 大小风格统一
- 间距统一
- 跳转逻辑统一

如果新增一个按钮，优先参考当前页面已有按钮样式，不要单独做一套完全不同的按钮视觉。

## 美术风格要求

本项目页面风格应保持为：

- 简约风
- 清爽
- 易读
- 不花哨
- 不堆砌装饰

### 推荐风格

- 以白色为主底色
- 使用浅绿色或微信按钮绿作为辅助色
- 色彩层次尽量少
- 留白充分
- 卡片和区块简洁清晰
- 文字层级明确，但不要过度设计

### 不推荐风格

- 过深的绿色
- 高饱和撞色
- 复杂渐变
- 过多阴影
- 大量装饰性文案
- 海报风、炫技风、强视觉冲击风格

### 颜色建议

如果需要使用绿色，优先使用接近微信按钮的绿色体系，并适当变浅：

- 主绿色可参考：`#07C160`
- 浅绿色背景建议使用更柔和的浅色
- 避免大面积使用深绿或墨绿

整体目标是“白绿色、简约、干净”。

## WXML / WXSS 修改建议

### WXML

- 模板结构尽量清晰
- 避免写过多重复结构
- 文案保持简洁直接
- 非必要不要加入多余标题、副标题、英文装饰文字

### WXSS

- 优先使用简单、统一的类名
- 优先做轻量优化，不做过重视觉包装
- 适当使用圆角、细边框、浅背景
- 阴影尽量轻

## 协作建议

在修改本项目时，默认优先级如下：

1. 功能正确
2. 按钮入口正确
3. 页面信息清晰
4. 风格保持简约统一
5. 再考虑局部美化

如果用户没有明确要求，请不要主动把页面改成复杂视觉风格。

## 当前风格结论

后续对本项目前端页面的修改，默认按照以下方向进行：

- 白绿色
- 简约风
- 按钮清晰
- 信息优先
- 少装饰
- 少炫技

